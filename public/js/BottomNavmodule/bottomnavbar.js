window.addEventListener('load',()=> {
    
    const notifsno = document.getElementById('noOfNotifs');
    const notifs = document.getElementById('profilenotif');

    notifsno.offsetLeft = notifs.offsetLeft;
})